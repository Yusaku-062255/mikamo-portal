# ミカモポータル 環境変数テンプレート
# このファイルをコピーして .env を作成し、実際の値を設定してください
# .env は Git 管理外です（.gitignore に含まれています）
#
# ⚠️ セキュリティ注意事項:
# - 実際のパスワードやAPIキーはこのファイル(.env.example)には絶対に書かないこと
# - .env ファイルをGitにコミットしないこと
# - 本番環境では Secret Manager を使用すること

# ============================================
# データベース設定
# ============================================
# 本番環境（Cloud Run）では DATABASE_URL 環境変数（Secret Manager経由）が必須
# ローカル開発環境では USE_LOCAL_DB=true を設定して、POSTGRES_* 環境変数を使用可能
DATABASE_URL=
USE_LOCAL_DB=false
# ⚠️ デフォルト値なし - 必ず独自の値を設定すること
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=mikamo_portal
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ============================================
# セキュリティ設定
# ============================================
# 本番環境では JWT_SECRET_KEY 環境変数（Secret Manager経由）を優先
JWT_SECRET_KEY=
SECRET_KEY=your-secret-key-change-in-production

# ============================================
# CORS設定
# ============================================
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# Anthropic (Claude) API設定【必須】
# ============================================
# ⚠️ 重要: APIキーは .env にだけ書くこと（コードやGitには絶対に入れない）
# 本番環境では Secret Manager 経由で設定（Secret名: MIKAMO_ANTHROPIC_KEY）
ANTHROPIC_API_KEY=
# カスタムエンドポイント（通常は設定不要）
# ANTHROPIC_API_BASE_URL=https://api.anthropic.com/v1/messages

# ============================================
# 3段階モデルティア設定（Anthropic Claude）
# ============================================
#
# 【BASIC】シフト管理、ログ要約など「軽めの業務オペレーション」用
# 用途: shift_planning, log_summary, simple_task, schedule
# コスト: 最も安価、レスポンス: 最速
# 推奨モデル: claude-3-haiku-20240307
ANTHROPIC_MODEL_BASIC=
ANTHROPIC_MAX_TOKENS_BASIC=500
ANTHROPIC_TEMPERATURE_BASIC=0.3

# 【STANDARD】従業員Q&A、ナレッジ検索＋回答など「ある程度ちゃんと考える」用
# 用途: staff_qa, knowledge_search, customer_support, daily_report
# コスト: 中程度、レスポンス: 適度
# 推奨モデル: claude-3-haiku-20240307 または claude-3-5-sonnet-20241022
ANTHROPIC_MODEL_STANDARD=
ANTHROPIC_MAX_TOKENS_STANDARD=1000
ANTHROPIC_TEMPERATURE_STANDARD=0.5

# 【PREMIUM】経営判断支援、DXレポート分析など「精度最優先の重めタスク」用
# 用途: management_decision, dx_report, strategic_planning, business_analysis
# コスト: 最も高価、レスポンス: 低速だが高精度
# 推奨モデル: claude-3-5-sonnet-20241022 または claude-3-opus-20240229
ANTHROPIC_MODEL_PREMIUM=
ANTHROPIC_MAX_TOKENS_PREMIUM=4000
ANTHROPIC_TEMPERATURE_PREMIUM=0.7

# ============================================
# 後方互換性のための設定（非推奨・将来削除予定）
# ============================================
# 新規実装では上記の3段階ティア設定を使用してください
AI_PROVIDER=anthropic
AI_MODEL=gpt-4o-mini
AI_PROVIDER_STAFF=anthropic
AI_MODEL_STAFF=
AI_MAX_TOKENS_STAFF=1000
AI_TEMPERATURE_STAFF=0.5

# OpenAI API（後方互換性のため）
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ============================================
# 初期管理者ユーザー設定
# ============================================
# 本番環境では Secret Manager から注入することを推奨
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=
INITIAL_ADMIN_FULL_NAME=
